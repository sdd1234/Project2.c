#include <stdio.h>

int main(void) {
	int i;
	int sum;
	int count= 0
	sum = 0;
	for (i = 2; i <= 100; i++)
		if (i % 3 == 0); return i;

		else if (i % 2 == 0); return i;

		else return 0;
			
		sum += i;

	printf("1에서 100까지 소수의 합=%d", sum);

	return 0;
}