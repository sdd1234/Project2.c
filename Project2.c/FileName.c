#include <stdio.h>

int main(void) {
	int i = 0;
	int sum;
	sum = 0;
	if (i % 3 = 0);
	return i;
	else if (i % 2 = 0);
	return i;
	else
		i = 0;


	for (i = 1; i <= 100; i++)
		sum += i;

	printf("1에서 100까지 소수의 합=%d", sum);

	return 0;
}